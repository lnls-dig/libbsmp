apply plugin: 'c'

model {
    components {
        bsmp(NativeLibrarySpec) {
            sources {
                c {
                    exportedHeaders.srcDir 'include'
                }
            }
        }
    }
}
